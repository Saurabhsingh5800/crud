<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="home.css">
    <title>crud operations</title>
    <style>
        th{
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="container" style="text-align: center; background-color: aqua;">
        <h1>ok</h1> 
    <h2>
        <%= locals.user.name %>
    </h2>      
    <ol style="text-decoration: none;">
        <% for (d of data){%>

            <li style="text-decoration: none; list-style-type: none;">
                <%= d.name %>
                <small>
                    <a class="delete-button" href="/user-delete/<%= d.id %>">delete</a>
                    <a class="delete-button" href="/user-update/<%= d.id %>">update</a>

                </small>

            </li>
            
        
        <%} %>
      
    </ol>
    <table>
        <tr>
            <th>Name</th>
            <th>Email</th>
          </tr>
        <% for (d of data){%>
        <tr>
            <th><%= d.name %></th>
            <th><%= d.email %></th>
        </tr>
        <%} %> 
    </table>
    <a href="/sign-out">logout</a>
    </div>
    
</body>
</html>